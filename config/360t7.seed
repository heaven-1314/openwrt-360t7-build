# ┌─────────────────────────────────────────────────────────────┐
# │           360T7 Custom Firmware — Package Selection          │
# ├─────────────────────────────────────────────────────────────┤
# │  This seed is appended after the first `make defconfig`.     │
# │  It adds all user-requested packages on top of the base      │
# │  ImmortalWrt system. Dependencies are auto-resolved.        │
# └─────────────────────────────────────────────────────────────┘

# ═══════════════════════════════════════════════════
#  daed — eBPF 透明代理 (ImmortalWrt 自带)
# ═══════════════════════════════════════════════════
CONFIG_PACKAGE_daed=y
CONFIG_PACKAGE_daed-geoip=y
CONFIG_PACKAGE_daed-geosite=y
CONFIG_PACKAGE_luci-app-daed=y
CONFIG_PACKAGE_luci-i18n-daed-zh-cn=y
CONFIG_PACKAGE_ca-bundle=y

# daed 内核依赖 (make defconfig 会自动补全，显式写出确保)
CONFIG_PACKAGE_kmod-sched-core=y
CONFIG_PACKAGE_kmod-sched-bpf=y
CONFIG_PACKAGE_kmod-veth=y
CONFIG_PACKAGE_kmod-xdp-sockets-diag=y
CONFIG_PACKAGE_kmod-nft-tproxy=y

# ═══════════════════════════════════════════════════
#  luci-app-turboacc — 网络加速
# ═══════════════════════════════════════════════════
CONFIG_PACKAGE_luci-app-turboacc=y
CONFIG_PACKAGE_luci-i18n-turboacc-zh-cn=y
# TurboAccel 依赖
CONFIG_PACKAGE_kmod-fast-classifier=y
CONFIG_PACKAGE_kmod-tcp-bbr=y

# ═══════════════════════════════════════════════════
#  luci-theme-argon — 主题
# ═══════════════════════════════════════════════════
CONFIG_PACKAGE_luci-theme-argon=y
CONFIG_PACKAGE_luci-app-argon-config=y
CONFIG_PACKAGE_luci-i18n-argon-config-zh-cn=y

# ═══════════════════════════════════════════════════
#  luci-app-statistics — 流量监控
# ═══════════════════════════════════════════════════
CONFIG_PACKAGE_luci-app-statistics=y
CONFIG_PACKAGE_luci-i18n-statistics-zh-cn=y
# collectd 模块
CONFIG_PACKAGE_collectd-mod-cpu=y
CONFIG_PACKAGE_collectd-mod-interface=y
CONFIG_PACKAGE_collectd-mod-iptables=y
CONFIG_PACKAGE_collectd-mod-load=y
CONFIG_PACKAGE_collectd-mod-memory=y
CONFIG_PACKAGE_collectd-mod-network=y
CONFIG_PACKAGE_collectd-mod-ping=y
CONFIG_PACKAGE_collectd-mod-thermal=y
CONFIG_PACKAGE_collectd-mod-uptime=y
CONFIG_PACKAGE_collectd-mod-conntrack=y
# RRD 图表渲染
CONFIG_PACKAGE_rrdcgi=y
CONFIG_PACKAGE_collectd-mod-rrdtool=y

# ═══════════════════════════════════════════════════
#  luci-app-adblock-fast — 广告过滤
# ═══════════════════════════════════════════════════
CONFIG_PACKAGE_luci-app-adblock-fast=y
CONFIG_PACKAGE_luci-i18n-adblock-fast-zh-cn=y

# ═══════════════════════════════════════════════════
#  基础系统增强
# ═══════════════════════════════════════════════════
# 中文支持
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y
CONFIG_PACKAGE_luci-i18n-opkg-zh-cn=y

# IPv6 (保持原生 relay 模式)
CONFIG_PACKAGE_odhcp6c=y
CONFIG_PACKAGE_odhcpd-ipv6only=y

# WiFi — WPA3/sae 支持 (匹配现有配置)
CONFIG_PACKAGE_wpad=y
# CONFIG_PACKAGE_wpad-basic-mbedtls is not set

# 实用工具
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_wget-ssl=y
CONFIG_PACKAGE_coreutils-base64=y
CONFIG_PACKAGE_bash=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_nano=y
CONFIG_PACKAGE_tcpdump=y

# ─────────────────────────────────────────────────────
#  以下选项确保系统行为与当前配置一致
# ─────────────────────────────────────────────────────

# DNS — dnsmasq
CONFIG_PACKAGE_dnsmasq=y

# 防火墙 — fw4 (nftables)
CONFIG_PACKAGE_firewall4=y
CONFIG_PACKAGE_nftables-json=y

# 不要多余的包（减小体积）
# CONFIG_PACKAGE_kmod-usb-net is not set
# CONFIG_PACKAGE_kmod-usb-storage is not set
